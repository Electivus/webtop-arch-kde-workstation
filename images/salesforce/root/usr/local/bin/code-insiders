#!/usr/bin/env sh
set -eu
editor="$HOME/.local/share/electivus/apps/code-insiders/current/bin/code-insiders"
if [ ! -x "$editor" ]; then
    printf '%s\n' 'VS Code Insiders is not ready. Run workstation-apps or use Prepare applications.' >&2
    exit 1
fi
exec "$editor" "$@"
